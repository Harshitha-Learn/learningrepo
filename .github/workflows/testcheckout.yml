name: sample

on:  
  workflow_dispatch:
 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:    
    - uses: actions/checkout@v3
    
    - name: List var
      run: |
         echo $GITHUB_SHA
         ECHO $GITHUB_REPOSITORY
